Here’s where things stand:
1. Gateway Global AI (/usr/gateway-global-ai)
Piece	Status
SMS gateway	Done. Inbound /webhook/sms, caller-ID → customer, 30-day conversation, outbound /api/sms/send, customers/conversations APIs.
Twilio provisioning	Done. services/twilio-provisioning.ts: search available numbers, list owned, buy, update webhooks, release.
REST API	Done. GET/POST/PATCH/DELETE /api/twilio/numbers/* with CORS for the TelephonyPanel.
CLI	Done. npm run twilio:provision -- search|list|buy|update|release (needs TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN).
Optional MCP server	Done. scripts/mcp-twilio-server.ts exposes Twilio tools; needs @modelcontextprotocol/sdk + zod.
Docs	Done. TELEPHONY_MODEL.md (tiers: 1 shared number vs paid dedicated), TWILIO_SETUP.md, TWILIO_INTEGRATION.md, MCP_TWILIO.md, deploy/guided-setup/preflight.
Still on you (if not done yet):
On the server (e.g. 72.61.4.44): Node/pm2, .env with TWILIO_AUTH_TOKEN, build + run app, DNS for sms.gatewayglobal.ai, SSL (Certbot), and point Twilio SMS webhook at https://sms.gatewayglobal.ai/webhook/sms.
2. ai-agent-twilio-telephony (TelephonyPanel app)
Piece	Status
Live provisioning in UI	Done. When VITE_TELEPHONY_API_URL points at Gateway Global (e.g. http://localhost:3002): Search = real Twilio search, Select = Buy & Select (real purchase), Release = real release.
Fallback	Done. Without that env var, panel uses mock search and local-only “provision”/release (no Twilio buy/release).
README	Done. Section on “TelephonyPanel + live Twilio provisioning” and VITE_TELEPHONY_API_URL.
3. Product / business model (in docs)
Initial setup: 1 shared platform number for SMS; caller-ID routing; no per-user number. Documented as the default, solid setup.
Business tier: Dedicated line for businesses that want the AI to SMS their customers; they pay for that integration; provisioning API + TelephonyPanel support it. Reflected in TELEPHONY_MODEL.md (tiers + checklist).
4. What’s not built yet (from earlier roadmap)
Project/task management – Projects, tasks, assignee, lifecycle (create → do → done).
Multi-agent create/manage – Create/edit/delete agents (DISC, ARCH, tools, telephony) in admin/API.
OpenClaw install + wiring – Actual openclaw onboard and wiring Gateway Global ↔ OpenClaw for agent execution/channels.
Voice webhook – SMS webhook is done; voice (TwiML/voice-call) for the same number is not implemented yet.
TL;DR:
SMS on one number (caller-ID routing) is implemented and documented. Dedicated-number provisioning (API + CLI + optional MCP) is in place, and the TelephonyPanel can do live search/buy/release when pointed at that API. The “1 shared number vs paid dedicated line” model is written down. Next steps are whatever you choose: server deploy, voice webhook, or project/task + multi-agent work.