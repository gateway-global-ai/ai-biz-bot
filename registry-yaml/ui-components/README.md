# UI component registry (YAML)

Machine-readable rows for [`UI_COMPONENT_APPROVAL_REGISTRY_V1.md`](../../docs-governance/canonical/UI_COMPONENT_APPROVAL_REGISTRY_V1.md).

- **`approved/`** — promoted components (`status: approved`).
- **`proposals/`** — in review (`status: proposed`).

Validate locally: *(validator script TBD — use PR review until `validate:ui-component-registry` exists).*
