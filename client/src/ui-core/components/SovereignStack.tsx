import Stack, { type StackProps } from "@mui/material/Stack";

export type SovereignStackProps = StackProps;

export function SovereignStack(props: SovereignStackProps) {
  return <Stack {...props} />;
}
