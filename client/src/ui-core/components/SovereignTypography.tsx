import Typography, { type TypographyProps } from "@mui/material/Typography";

export type SovereignTypographyProps = TypographyProps;

export function SovereignTypography(props: SovereignTypographyProps) {
  return <Typography {...props} />;
}
