node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment files (never commit secrets) — Gate 1: Perimeter Guard
# CTO WARNING: The ONLY env file committed to the repo is .env.example. It is DOCUMENTATION ONLY.
# It does not drive production or CI; real values live in Doppler (and optionally server .env). See docs/GITHUB_SECRET_MANIFEST.md.
.env
.env.local
.env.*.local
.env.production
.env.staging
env.local
docs/knowledge-base/.env.local
**/.env.local

# Governance daily run outputs (npm run governance:daily); keep folder README only
docs-governance/artifacts/daily/**
!docs-governance/artifacts/daily/README.md

# Generated shadcn.io catalog HTTP probe (npm run shadcn-io:health)
docs-governance/artifacts/shadcn_io_catalog_health_report.json

# Build artifacts
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories
.idea
.vscode

# Cursor MCP config (contains secrets; use .cursor/mcp.example.json as template)
.cursor/mcp.json
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Temporary files
/tmp
*.tmp
*.har

# User uploads (may contain Doppler/secret exports — do not commit)
user_uploads/

# Website Builder SDK build artifacts and dependencies
sdk/website-builder/node_modules/
sdk/website-builder/dist/

# Optional: zip assets used during build (e.g. Voice Lead Machine source)
*.zip
reference/attached_assets/*.zip

# Generated Nova RSA keys (dev only; add to Doppler, do not commit)
.nova-keys-generated/

# Runtime/generated uploads
server/uploads/
